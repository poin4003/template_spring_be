<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.template.app.features.rbac.repository.PermissionRepository">

    <select id="selectPermissionByUserId" resultType="com.template.app.features.rbac.entity.PermissionEntity">
        SELECT DISTINCT p.permission_id, p.permission_name, p.permission_key
        FROM permissions p
        JOIN role_permissions rp ON p.permission_id = rp.permission_id
        JOIN user_roles ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="selectPermissionByRoleId" resultType="com.template.app.features.rbac.entity.PermissionEntity">
        SELECT p.permission_id, p.permission_name, p.permission_key
        FROM permissions p
        JOIN role_permissions rp ON p.permission_id = rp.permission_id
        WHERE rp.role_id = #{roleId}
    </select>

</mapper>
